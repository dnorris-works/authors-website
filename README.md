# Deep Field Press

Multi-tenant publishing platform built with Next.js. Serves different author websites based on the incoming domain.

## Development

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) to see the result.

## Build & Deploy

```bash
npm run build
npm run start
```

Deployment is configured via `nixpacks.toml`.
